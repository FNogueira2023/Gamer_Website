<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Article</title>

    <script src="https://kit.fontawesome.com/359284d803.js" crossorigin="anonymous"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=News+Cycle&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/stylesheets/mediaQueries.css">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/article.css">

</head>

<body>

    <!-- header -->


    <%- include("./partials/header.ejs") %>


        <!-- main -->

        <section class="article">
            <h1>
                <%= article.title %>
            </h1>

            <h2>
                <%= article.subtitleA %>
            </h2>

            <p>
                <%= article.date %>
            </p>


            <div class="articleBody">
                <div class="articleMain">
                    <div>
                        <% for( let i=0; i < 4; i++ ) { %>
                            <p>
                                <%= article.paragraphsA[i] %>
                            </p>
                            <% } %>
                    </div>

                    <div class="video">
                        <iframe width="100%" height="800" src=<%=article.videoA %>
                            title="YouTube video player" frameborder="0"
                            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope;
                            picture-in-picture"
                            allowfullscreen></iframe>
                    </div>

                    <h2>
                        <%= article.subtitleB %>
                    </h2>

                    <div>
                        <% for( let i=0; i < 3; i++ ) { %>
                            <p>
                                <%= article.paragraphsB[i] %>
                            </p>
                            <% } %>
                    </div>

                    <img src="/images/<%= article.imageB %> " alt="">


                    <div>
                        <% for( let i=0; i < 4; i++ ) { %>
                            <p>
                                <%= article.paragraphsC[i] %>
                            </p>
                            <% } %>
                    </div>

                    <img src="/images/<%= article.imageC %> " alt="">

                    <div>
                        <% for( let i=0; i < 3; i++ ) { %>
                            <p>
                                <%= article.paragraphsD[i] %>
                            </p>
                            <% } %>
                    </div>
                </div>
            </div>
        </section>

        <!-- footer -->

        <%- include("./partials/footer.ejs") %>


</body>




</html>