<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login or Signup</title>

    <script src="https://kit.fontawesome.com/359284d803.js" crossorigin="anonymous"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=News+Cycle&display=swap" rel="stylesheet" />

    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/mediaQueries.css" />
    <link rel="stylesheet" href="/stylesheets/loginRegister.css">
</head>

<body>

    <!-- header -->

    <%- include("./partials/header.ejs") %>

        <div class="container">

            <div class="main">
                <input type="checkbox" id="chk" aria-hidden="true">

                <div class="signup">
                    <form action="/users/create" method="post">
                        <label for="chk" aria-hidden="true">Sign up</label>
                        <input type="hidden" name="formType" value="signup">
                        <input type="text" name="userName" placeholder="User name"
                            value="<%=locals.old && old.user ? old.user :" User name" %>" required>
                        <input type="email" name="email" placeholder="Email"
                            value="<%= locals.old && old.user  ? old.email :" Email" %>" required>
                        <input type="password" name="pswd" placeholder="Password"
                            value="<%= locals.old && old.pswd ? old.pswd :" Password" %>" required>
                        <button type="submit">Sign up</button>

                    </form>
                </div>

                <div class="login">
                    <form action="/users/login" method="post">
                        <label for="chk" aria-hidden="true">Login</label>
                        <input type="hidden" name="formType" value="login">
                        <input type="email" name="email" placeholder="Email"
                            value="<%= locals.old && old.email  ? old.email : " Email" %>" required>
                        <input type="password" name="pswd" placeholder="Password" required>
                        
                        <button type="submit">Login</button>
                    </form>
                </div>
            </div>
        </div>



        <div id="errorsMessage">
            <% if (locals.errors) {%>
                <div>
                    <% errors.forEach(error=> { %>
                        <p>
                            <%= error.msg %>
                        </p>
                        <% }) %>
                </div>
                <% } %>
        </div>




        <!-- footer -->

        <%- include("./partials/footer.ejs") %>

</body>

</html>